//
// Created by Tayo Oduyemi on 2020-12-04.
//
#include <iostream>

using namespace std;
constexpr int arr[]{4,6,7,3,8,2,1,9,5};
/* int main() {
    int n;
    do {
        cout << "Enter a number b/n  1 and 9" << endl;
        cin >> n;
        if(cin.fail()) {
            cin.clear();
            cin.ignore(32767, '\n');
        }
    } while(n > 9 || n < 1);
    for(int i = 0; i < size(arr); ++i) {
        if(n == arr[i]) {
            cout << "At index " << i << endl;
            break;
        }
    }
} */